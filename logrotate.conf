/opt/xray-dns/logs/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    create 600 root root
    sharedscripts
    postrotate
        systemctl kill -s SIGUSR1 xray-dns.service 2>/dev/null || true
    endscript
}
